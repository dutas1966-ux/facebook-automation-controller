Each module should implement a small adapter with:

    run(account, limit)

Keep credentials/session data outside the repository.

Platform-specific behavior should use permitted APIs or other compliant mechanisms.
